package setting

/*
const settingFilePath string = "./setting/setting.json"

func Init() {
	readSettingFile()
}

func readSettingFile() {
	//system := logging.NewSystemLogger()

	file, err := os.ReadFile(settingFilePath)
	if err != nil {
		system.Warn(err)
	}

	err = json.Unmarshal(file, &Setting)
	if err != nil {
		system.Warn(err)
	}
}
*/
